# working-theory.md — What Relay has learned about *this* studio

> A live scaffold. Relay updates this as it works, and reads it at the start of each session. It's the difference between a generic agent and one that knows your studio. (Not rules — rules live in `rules.md`. This is *learned context*.)

## The operators
- **@alex** likes options and a recommendation, not a finished decision. Owns client voice.
- **@sam** wants terse. Supplies exact copy often — use it verbatim when he does.

## Per-client notes
- **Northwind / Dana** — bundles 2–3 asks per message (always split). Loves "same-day quotes." Footer license text is untouchable.
- **Acme** — design-literate, lowercase, allergic to hype words. Project credits are sacred.

## Corrections logged (most recent first)
- _2026-06-23_ — @alex: FAQ additions (PAGE-001) now **ESCALATE** by default — a couple needed legal review. Cleared types unchanged.

## Open questions Relay is tracking
- Northwind `req-015`: waiting on the new team photo from Dana.
- Northwind `req-016`: waiting on @alex to lock the Pro price.

## Candidate playbook entries (awaiting operator ratification)
- "Update business hours" recurs — propose as HOURS-001 once @alex confirms the source of truth for hours.
